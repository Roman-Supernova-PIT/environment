=======================
Roman SN Pit Enviroment
=======================

Provides a conda environment (in ``sn_pit_dev.yaml``) and a docker
environment (defined in ``docker/Dockerfile``).

For more information about the Docker images, see the README in the
``docker`` directory.
