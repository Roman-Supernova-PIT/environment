system:
  db:
    url: https://romansnpit-ricksim-smdc.lbl.gov
    username: dbuser
    # LEAVE PASSWORD AS null.  Do NOT edit this field to put in a password!  Just use passwordfile
    password: null
    passwordfile: /secrets/roman_snpit_db_ricksim_smdc

  paths:
    spectra1d: /data/spectra1d
    lightcurves: /data/lightcurves
    segmaps: /data/segmaps
    images: /data/images

    temp_dir: /snpit_temp
    dev_storage: /dev_storage

    snappl:
      A25ePSF_path: /dev/null

  # ...gotta figure out where these are and bind mount them in singrun_smdc_ricksim.sh
  ou24:
    simdex_server: https://roman-desc-simdex.lbl.gov
    tds_base: /dev/null
    config_file: /snpit_env/environment_checkout_for_apptainer/ou2024_tds.yaml
    sn_truth_dir: /dev/null
    images: /dev/null
    sims_sed_library: /dev/null
